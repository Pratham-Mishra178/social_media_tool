<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Dashboard - Social Media Tool</title>
  <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
</head>
<body class="bg-gradient-to-b from-pink-300 to-pink-100 font-sans">
  <% content_for :navbar do %>
  <!-- Navbar -->
  <nav class="bg-blue-600 text-white shadow-lg">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="flex justify-between items-center h-16">
        <div class="flex space-x-4">
          <a href="#" class="text-lg font-bold">Dashboard</a>
          <%= link_to "Home", root_path, class: "hover:bg-blue-500 px-3 py-2 rounded-md" %>
          <%= link_to "Settings", settings_path, class: "hover:bg-blue-500 px-3 py-2 rounded-md" %>
          <%= link_to "Posts Dashboard", posts_dashboard_path, class: "hover:bg-blue-500 px-3 py-2 rounded-md" %>
        </div>
        <div>
          <%= button_to "Logout", root_path, method: :get, class: "py-2 px-4 bg-red-500 hover:bg-red-700 text-white rounded-lg" %>
        </div>
      </div>
    </div>
  </nav>
<% end %>

<!-- Main Dashboard Section -->
<div class="flex justify-center items-center min-h-[calc(100vh-4rem)]">
  <div class="max-w-md w-full p-8 rounded-lg shadow-lg bg-white bg-opacity-50 backdrop-blur-md border border-gray-200">
    <h1 class="text-3xl font-bold text-gray-900 mb-4">Scheduler App</h1>
    <!-- Navigation to Post Dashboard -->
    <p class="mb-4 text-center text-gray-800">Manage your scheduled posts easily:</p>
    <%= link_to "Go to Posts Dashboard", posts_dashboard_path, class: "w-full py-2 px-4 bg-blue-500 hover:bg-blue-700 text-white rounded-lg text-center inline-block" %>
  </div>
</div>

</body>
</html>
